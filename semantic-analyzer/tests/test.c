float function(int a) {
   int b;
   // b = 6.589 + 0.001;
   a = 10.5 + 6 * 5;
   b = 6.589 + 1;
   // a = a / a + a;
   // return a + b;
}

// int f(,int a) { x = x + 1; }
// int f(,int a) { x = f(x) + 1; }
// int f(int a) { x = f(x,y) + 1; }

// int x;

int main(){
   // if (1<3) $x+1;else x=1; else x = 1;
   // a - b -c;
   // << l;
   // l >>;
   // int list help;
   // help : NIL;

   function(1);

   // float list i;
   // // int i;
   // for (i = 0; ; i = i+1) {
   //    writeln(i);
   //    x = 9.65;
   // }
   return 0;
}