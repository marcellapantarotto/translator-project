int main() {
   // Comment
   float list my_list;
   int list other_list;

   if(my_list == other_list) {
      write("Lists are the same");
   } 
   if(my_list != other_list) {
      writeln("Lists are not the same");
   }
}